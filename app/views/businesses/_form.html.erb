<%= simple_form_for(business, wrapper: :horizontal_form) do |form| %>
  <%= form.input :name, disabled: show_action?  %>
  <%= form.input :abn,  disabled: show_action?, label: "ABN" %>
  <div class="form-group row mb-0">
    <div class="mx-auto">
      <% if !show_action? %>
        <%= form.button :submit, class: "btn-primary" %>
      <% end %>
      <%= link_to 'Cancel', businesses_path, class: 'btn btn-outline-secondary' %>
    </div>
  </div>
<% end %>
